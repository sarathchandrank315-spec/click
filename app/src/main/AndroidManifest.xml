<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AiFA">
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.AiFA">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver
            android:name=".reminder.ReminderReceiver"
            android:exported="false" />

        <activity
            android:name=".ui.register.RegisterActivity"
            android:exported="true"
            android:theme="@style/Theme.AiFA"></activity>
        <activity
            android:name=".reminder.ReminderActivity"
            android:exported="true"
            android:theme="@style/Theme.AiFA"></activity>
        <activity
            android:name=".ui.register.FamilyRegistrationActivity"
            android:exported="true"
            android:theme="@style/Theme.AiFA"></activity>
        <activity
            android:name=".ui.dashBoard.HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.AiFA"></activity>
        <activity
            android:name=".ui.addTransaction.AddTransactionActivity"
            android:exported="true"
            android:theme="@style/Theme.AiFA"></activity>
        <activity
            android:name=".ui.dashBoard.chatbot.ChatBotActivity"
            android:exported="true"
            android:theme="@style/Theme.AiFA"></activity>
        <activity
            android:name=".ui.addTransaction.addIncome.AddIncomeActivity"
            android:exported="true"
            android:theme="@style/Theme.AiFA"></activity>

    </application>

</manifest>